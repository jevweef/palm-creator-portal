.env.local
.env
node_modules
.next
.vercel
.env.local.spotify
.mcp.json

# Nested inspo-pipeline repo (managed separately, pushes to github.com/jevweef/inspo-pipeline)
pipeline/

# Claude Code session/worktree state — never deploy
.claude/worktrees/
.claude/jobs/
