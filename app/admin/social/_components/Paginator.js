'use client'

// Paginator — single pagination control for the hub. Re-exports the existing
// LibPickerPaginator so there's one paginator implementation app-wide.
export { LibPickerPaginator as default } from '@/components/EditorDashboard'
