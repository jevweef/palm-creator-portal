'use client'

// ContentCard — the hub's content tile. Re-exports the existing LibraryCard so
// the Creator Library, review grids, and any future content grid share ONE
// card implementation and visual language.
export { LibraryCard as default } from '@/components/EditorDashboard'
